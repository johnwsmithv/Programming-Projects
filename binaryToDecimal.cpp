#include <iostream>
#include <stdio.h>     
#include <stdlib.h>   
#include <string>
#include <vector>
class bianaryToDecimal{
    std::vector<int> binaryArray;
    int arrayTwo[64];
    public:
        long decimal;
        long binary;
        long hex;

    int decimalHexToBinary(char *arguement){
        int input = strtoul(arguement, NULL, 0);
        while(input > 0.5){
            binaryArray.insert();
        }
    }
};